program MaxMinBorder;

var
  x: int64;
  Dest: array[0..10] of integer;
  y: integer;
begin
  y := 12; // Инициализация переменной y
  WriteLn(Low(x));  // Минимальное значение типа Int64
  WriteLn(High(x)); // Максимальное значение типа Int64
  writeln(sizeof(Dest)); // Размер массива в байтах
  writeln(length(dest)); // Длина массива
  writeln(sizeof(y)); // Размер переменной y
end.
